---
title: Implementation Notes
description: Deep dive into the internal mechanics of @ilokesto/state.
---

# Implementation Notes

The state hook subscribes, compares selected values, dispatches reducer actions, and avoids repeated persistence writes through observable store behavior.

## How the hook subscribes

The hook returned by `create` subscribes to the store and reads snapshots through React's external store API.

- **Selector Logic**: Each hook call establishes its own store subscription, then applies the selector to the current snapshot before returning a value.
- **Server Snapshot**: `getServerSnapshot` is derived from `store.getInitialState()`, which is how SSR stays aligned with the original constructor value.

## Comparison and Performance

- **Store Layer**: The underlying `Store` still uses `Object.is` to decide whether an update should notify listeners at all.
- **State Layer**: The hook compares the previous store snapshot with the next one by value. When the snapshots are equivalent, it reuses the previous selected value instead of returning a new reference.
- **No Custom Equality API**: There is no separate selector equality argument in the current public API.

## Reducer Mode

When you use `create(reducer, initialState)`, reducer actions pass through the same middleware path as state updates.

- **Action Dispatching**: The returned `dispatch` function sends an action through the middleware chain, where the reducer eventually processes it into a new state.
- **Action Name Stamping**: During reducer dispatch, the store exposes the current action name long enough for logger and devtools middleware to label the update.
- **Extensibility**: Because it uses the same middleware system, you can combine reducer-based stores with logging, persistence, and validation.

## Write Cache (Persistence)

To avoid excessive I/O, the `persist` middleware maintains a write cache.

- **Deduplication**: It compares the new state with the last written state. If they are identical, the write operation is skipped.
- **Debounced Writes**: You can combine `persist` with `debounce` to further reduce storage pressure.
