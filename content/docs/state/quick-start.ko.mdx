---
title: 빠른 시작 (Quick Start)
description: 몇 분 만에 @ilokesto/state를 시작하세요.
---

# 빠른 시작 (Quick Start)

선호하는 패키지 매니저를 사용하여 패키지를 설치합니다:

```package-install
@ilokesto/state
```

## 기본 사용법

스토어를 생성하고 컴포넌트에서 사용하는 방법은 매우 간단합니다.

```tsx
import { create } from '@ilokesto/state';

// 1. 스토어 훅 생성
const useCounter = create({ count: 0 });

function Counter() {
  // 2. 셀렉터와 함께 훅 사용
  const [count, setState] = useCounter((state) => state.count);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setState((prev) => ({ ...prev, count: prev.count + 1 }))}>
        Increment
      </button>
    </div>
  );
}
```

## 전역 액션

`writeOnly` 접근자를 사용하여 컴포넌트 외부에서 액션을 정의할 수 있습니다.

```ts
const useCounter = create({ count: 0 });

export const increment = () => {
  const setState = useCounter.writeOnly();
  setState(state => ({ count: state.count + 1 }));
};
```

## 다음 단계는?

- 라이브러리 이면의 [정신 모델 (Mental Model)](./mental-model)을 이해하세요.
- [핵심 API](./create)에 대해 자세히 알아보세요.
- 로깅, 영속화 등을 위한 [미들웨어](./middleware)를 살펴보세요.
