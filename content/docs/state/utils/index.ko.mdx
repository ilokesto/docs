---
title: 유틸리티
description: "@ilokesto/state 유틸리티 헬퍼 개요."
---

# 유틸리티

`@ilokesto/state/utils` 엔트리포인트는 현재 `pipe`와 `adaptor` 두 가지 헬퍼를 제공합니다.

이 둘은 서로 다른 문제를 해결합니다.

- [`pipe`](./pipe)는 plain initial state를 기준으로 여러 미들웨어를 조합해 `Store`를 만듭니다.
- [`adaptor`](./adaptor)는 Immer의 `produce()`를 감싸서 draft 스타일 객체 업데이트를 쉽게 만듭니다.

## 언제 유틸리티를 쓰면 좋은가

유틸리티는 `create()`의 공개 API를 바꾸지 않으면서, 스토어 설정이나 객체 업데이트의 사용성을 개선하고 싶을 때 쓰는 것이 좋습니다.

## 관련 API

- [`create`](../create): React 통합을 위한 메인 진입점.
- [`미들웨어`](../middleware): 일반적인 작업을 위한 내장 미들웨어.
