---
title: 리듀서
description: "@ilokesto/state에서 복잡한 상태 전이를 위해 리듀서 사용하기."
---

# 리듀서

복잡한 상태 전이가 필요한 경우, `@ilokesto/state`는 React의 `useReducer` 또는 Redux와 유사한 리듀서 기반 모드를 지원합니다.

## 리듀서 정의

리듀서는 현재 상태와 액션을 인자로 받아 다음 상태를 반환하는 순수 함수입니다.

```ts lineNumbers
import { create } from '@ilokesto/state/react';

type State = { count: number };
type Action = { type: 'increment' } | { type: 'decrement' } | { type: 'set'; value: number };

const counterReducer = (state: State, action: Action): State => {
  switch (action.type) {
    case 'increment': return { count: state.count + 1 };
    case 'decrement': return { count: state.count - 1 };
    case 'set': return { count: action.value };
    default: return state;
  }
};

const useCounter = create(counterReducer, { count: 0 });
```

## 액션 디스패치

리듀서 모드에서 훅은 `setState` 대신 `dispatch` 함수를 반환합니다.

```tsx lineNumbers
function Counter() {
  const [count, dispatch] = useCounter(state => state.count);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => dispatch({ type: 'increment' })}>Increment</button>
      <button onClick={() => dispatch({ type: 'decrement' })}>Decrement</button>
    </div>
  );
}
```

## 전역 디스패치

React 외부의 코드에서 액션을 디스패치해야 할 때는 `writeOnly` 접근자로 `dispatch` 함수를 얻습니다.

```ts lineNumbers
export const incrementCount = () => {
  const dispatch = useCounter.writeOnly();
  dispatch({ type: 'increment' });
};
```

## 리듀서 모드를 사용할 때

- 하나의 액션이 여러 필드를 함께 바꿔야 할 때 리듀서 모드를 사용합니다.
- 다음 상태를 반환하기 전에 각 전이에 검증이나 분기가 필요할 때 사용합니다.
- `logger`나 `devtools` 미들웨어에서 이름 있는 액션으로 디버깅을 더 분명하게 만들고 싶을 때 사용합니다.
- 기존 `useReducer` 로직을 큰 형태 변경 없이 공유 스토어로 옮길 때 사용합니다.

## 리듀서 모드 동작

리듀서는 항상 **전체** 상태 객체를 반환해야 합니다. 리듀서 모드에서 부분 상태 업데이트는 지원되지 않습니다.

리듀서 기반 스토어는 모든 `@ilokesto/state` 미들웨어를 지원합니다.
