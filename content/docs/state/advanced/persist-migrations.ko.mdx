---
title: "Persist migration"
description: "persist의 storage type, versioning, migration caveat입니다."
---

# Persist migration

`persist`는 `{ state, version }`을 browser storage에 serialize합니다. version은 migration array 길이입니다.

## Storage type

- `{ local: key }`는 `localStorage`를 사용합니다.
- `{ session: key }`는 `sessionStorage`를 사용합니다.
- `{ cookie: key }`는 `document.cookie`를 사용합니다.

## Migration

migration function은 stored version index부터 최신 migration까지 실행됩니다. 결과는 새 version과 함께 다시 저장됩니다.

```ts lineNumbers
persist({ theme: 'light', count: 0 }, {
  local: 'settings',
  migrate: [
    (old) => ({ theme: String(old), count: 0 }),
    (old) => ({ ...old, count: Number(old.count ?? 0) }),
  ],
});
```

## 주의할 점

- migration은 `local`과 `cookie` storage에서만 시도됩니다.
- `session` storage는 public type에서 `migrate`를 받지 않습니다.
- 값은 JSON parse/stringify됩니다. JSON이 아닌 state는 persistence 전에 변환하세요.
- browser storage 실패는 `window`가 있을 때만 catch 후 log됩니다.
