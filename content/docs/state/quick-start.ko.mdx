---
title: 빠른 시작
description: 설치부터 첫 store hook까지 가장 짧은 경로.
---

# 빠른 시작

store hook을 만들면 React 컴포넌트가 읽고 갱신할 수 있는 작고 타입이 잡힌 상태 소스가 생깁니다.

선호하는 패키지 매니저를 사용하여 패키지를 설치합니다:

```package-install
@ilokesto/state
```

## 1. store hook 만들기

가장 작은 `@ilokesto/state` 사용 예제는 초기 상태 객체에서 시작합니다.

```tsx lineNumbers
import { create } from '@ilokesto/state';

const useCounter = create({ count: 0 });
```

## 2. slice를 읽고, 전체 상태를 쓴다

셀렉터는 컴포넌트가 무엇을 읽을지를 정합니다. 하지만 반환되는 쓰기 함수는 여전히 전체 스토어 상태를 갱신합니다.

```tsx lineNumbers
function Counter() {
  const [count, setState] = useCounter((state) => state.count);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setState((prev) => ({ ...prev, count: prev.count + 1 }))}>
        Increment
      </button>
    </div>
  );
}
```

이 지점이 처음엔 가장 헷갈리기 쉽습니다. 셀렉터가 `count`만 반환하더라도, 쓰기 함수는 이전 전체 상태를 받아 다음 전체 상태를 만들어야 합니다.

## 3. 쓰기가 로컬 이벤트를 넘어서면 컴포넌트 밖으로 뺀다

업데이트 로직이 버튼 핸들러를 넘어서 “액션”처럼 느껴지기 시작하면 `writeOnly()` 뒤로 빼는 편이 좋습니다.

```ts lineNumbers
const useCounter = create({ count: 0 });

export const increment = () => {
  const setState = useCounter.writeOnly();
  setState(state => ({ count: state.count + 1 }));
};
```

이렇게 하면 컴포넌트는 더 작아지고, store 동작은 재사용하거나 테스트하기 쉬워집니다.

## 다음 단계는?

- [멘탈 모델](./mental-model): store가 React 바깥에 있는 이유.
- [`create`](./create): 리듀서 모드를 포함한 호출 시그니처.
- [셀렉터](./selectors): 튜플 뒤의 구독과 메모이제이션.
- [접근자](./accessors): 컴포넌트 밖 액션과 읽기.
- [기존 스토어 사용](./existing-store): vanilla `Store`에 hook 계층 더하기.
- [미들웨어](./middleware): 로깅, 영속화, 검증, DevTools 지원.
