---
title: "Fetcher"
description: "ky의 런타임 사용감을 유지하는 얇은 OpenAPI-aware facade입니다."
---

# Fetcher

`@ilokesto/fetcher`는 실제 `ky` 런타임 위에 얇게 얹히는 OpenAPI-aware wrapper입니다. `ky.create`, `extend`, hooks, `prefixUrl`, custom `fetch`, lazy `ResponsePromise` parsing, `ky` error를 유지하면서 typed route template, grouped shortcut request, `.json()` 결과 추론, throw하지 않는 `safe` surface를 추가합니다.

## 설치

`fetcher`는 peer dependency인 `ky`와 함께 설치합니다.

```bash
pnpm add @ilokesto/fetcher ky
```

패키지는 dist 기반 ESM package로 배포됩니다. 패키지 tooling 기준은 Node.js 22 이상이며, 실제 HTTP 런타임은 `ky`에 위임하므로 browser, server, test 환경 제약은 사용하는 `ky` 환경과 같습니다.

## 빠른 예시

```ts lineNumbers
import { createFetcher, type MergePaths } from '@ilokesto/fetcher/openapi';
import type { paths as GeneratedPaths } from './__generated__/openapi';

type ExtraPaths = {
  '/bff/checkout/quote': {
    post: {
      requestBody: {
        content: {
          'application/json': {
            cartId: string;
            couponCode?: string;
          };
        };
      };
      responses: {
        200: {
          content: {
            'application/json': {
              total: number;
              currency: 'KRW' | 'USD';
              discountApplied: boolean;
            };
          };
        };
      };
    };
  };
};

type ApiPaths = MergePaths<GeneratedPaths, ExtraPaths>;

const api = createFetcher<ApiPaths>({
  prefixUrl: '/api',
});

const user = await api
  .get('/users/{id}', {
    params: {
      path: { id: '42' },
      query: { include: 'profile' },
    },
  })
  .json();

const quote = await api
  .post('/bff/checkout/quote', {
    json: { cartId: 'cart_123', couponCode: 'WELCOME10' },
  })
  .json();
```

## 다음에 볼 문서

- [Quick start](/ko/fetcher/quick-start)에서 typed client를 처음부터 만듭니다.
- [Core concepts](/ko/fetcher/core-concepts)에서 ky-first model을 설명합니다.
- [Entrypoints](/ko/fetcher/reference/entrypoints)에서 public import surface를 확인합니다.
- [Grouped request](/ko/fetcher/reference/request-grouped-request)에서 shortcut request shape를 다룹니다.
- [Safe](/ko/fetcher/reference/safe)에서 throw하지 않는 request를 설명합니다.
