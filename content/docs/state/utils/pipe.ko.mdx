---
title: pipe
description: plain initial state에 미들웨어를 조합합니다.
---

# `pipe`

`pipe`는 plain initial state로 `Store`를 만들고, 미들웨어를 순서대로 적용한 뒤 초기화된 store를 반환합니다.

```ts lineNumbers
import { create } from '@ilokesto/state';
import { pipe } from '@ilokesto/state/utils';
import { logger, devtools } from '@ilokesto/state/middleware';

const store = pipe(
  { count: 0 },
  logger(),
  devtools('MyStore')
);

const useStore = create(store);
```

## 동작 방식

- plain initial state에서 시작합니다.
- vanilla `Store`를 생성합니다.
- 미들웨어를 왼쪽에서 오른쪽으로 적용합니다.
- 결과 `Store`를 반환하고, 이를 `create(store)`에 전달할 수 있습니다.

## 언제 쓰면 좋은가

스토어에 둘 이상의 미들웨어가 필요하고 중첩 호출이 적용 순서를 가릴 때 `pipe`를 사용합니다.

## 관련 API

- [`유틸리티`](./): 유틸리티 개요.
- [`미들웨어`](../middleware): 내장 미들웨어.
- [`create`](../create): 조합된 스토어를 React에 연결하는 API.
