---
title: Angular
description: signal과 DestroyRef로 @ilokesto/state/angular를 사용하는 방법.
---

# Angular

Angular 어댑터 API는 `@ilokesto/state/angular`에서 가져옵니다.

```ts lineNumbers
import { Component, DestroyRef, inject } from '@angular/core';
import { create } from '@ilokesto/state/angular';

type CounterState = { count: number };

const counter = create<CounterState>({ count: 0 });

@Component({
  selector: 'app-counter',
  standalone: true,
  template: '<button (click)="increment()">{{ count() }}</button>',
})
export class CounterComponent {
  private readonly destroyRef = inject(DestroyRef);
  readonly count = counter((state) => state.count, { destroyRef: this.destroyRef }).state;

  increment() {
    counter.writeOnly()((prev) => ({ ...prev, count: prev.count + 1 }));
  }
}
```

## 반환 형태

Angular 어댑터는 signal을 만드는 함수를 반환합니다.

- state mode: `{ state: Signal<S>, setState, subscribe }`
- reducer mode: `{ state: Signal<S>, dispatch, subscribe }`

## Lifecycle 규칙

활성 injection context 밖에서 Angular signal을 만들면 `{ destroyRef }`를 명시적으로 넘기세요. 어댑터는 그 scope로 store 구독을 정리합니다.
