---
title: "useDebounce"
description: "빠르게 변하는 값을 debounce합니다."
---

# useDebounce

`useDebounce`는 `delay` ms 동안 추가 변경이 없을 때만 갱신되는 debounced 값을 반환합니다.

```tsx lineNumbers
import { useState, useEffect } from 'react';
import { useDebounce } from '@ilokesto/utilinent/hooks';

function Search() {
  const [query, setQuery] = useState('');
  const debounced = useDebounce(query, 300);
  useEffect(() => search(debounced), [debounced]);
  return <input value={query} onChange={(e) => setQuery(e.target.value)} />;
}
```

초기값은 즉시 반환되고, 이후 변경은 지연됩니다.